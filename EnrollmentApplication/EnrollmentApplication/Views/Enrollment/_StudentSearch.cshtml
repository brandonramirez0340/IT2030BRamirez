
@model IEnumerable<EnrollmentApplication.Models.Student>

<div id="searchresults">
    <b>Student Search Results:</b>
    <ul>
        @foreach (var item in Model)
        {
            <li>@item.LastName, @item.FirstName</li>
        }
    </ul>
</div>